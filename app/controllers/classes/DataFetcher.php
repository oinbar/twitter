// Extends DataPusher. Creates a base document for future transformations.

Throws DataFetcherException


constuctor(transfomrations list) 


documents must end up with:

type
datetime (full format)
transformations ({ transformation : {name : "", date: "323423423"/blank=not completed yet}, })

methods:
	public run : returns document